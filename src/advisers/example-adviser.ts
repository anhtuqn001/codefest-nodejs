import { IMapInfo } from "../types/node";

const exampleAdviser = (mapInfo: IMapInfo) => {
    // if there is bom nearby
    // mainTaskStackSubject.next() => send 1 action avoidBomb to the main stack.
}

export default exampleAdviser;